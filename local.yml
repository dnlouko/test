---
- name: Playbook de teste AWX
  hosts: ubuntu
  tasks:
  - name: remove package {{ pacotinho }}
    apt:
      name: "{{ pacotinho }}"
      state: latest
  - name: Update all packages to their latest version
    apt:
      name: "*"
      state: latest
...
